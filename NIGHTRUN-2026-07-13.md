# Night Run — 2026-07-13 (Paris Portfolio)

## What this is
Oscar's Next.js 16 personal portfolio (the "rework"; deploy target `oscarmorke.com`), with four Claude-named design directions (fable / haiku / opus / sonnet) + a `/compare` page. True state going in: built but **not deployed**, carrying false numbers its own locked CV disowns (per the Jul 13 system audit), format not yet chosen.

## Shipped
Branch **`nightrun/portfolio-hygiene-2026-07-13`** (NOT pushed). Two commits:
- **`07ba195` number-hygiene** (`data.ts` + `PROJECT.md`) — aligned every claim to mindmap §13 IDENTITY:
  - 20M users → **8M+ devices** (Ledger)
  - $115K / 39.05 ETH → **$188K+ / ~42 ETH** (team prizes)
  - Yieldbound 2nd/646 → **2nd/687**
  - RECEIPT "ethglobal finalist" → **top 20 / 468** ("finalist" is banned for RECEIPT)
  - ArbiGates "#1 winner" / "won arbitrum's first hackathon" → **partner golds (~3rd overall)**
  - Kept: NFT Safe Launch + BriefMCP "finalist" (both genuinely were).
- **`cce7ed7` build-root fix** (`next.config.ts`) — pinned `turbopack.root` to the project dir so the build stops selecting `~/package-lock.json` (the home-dir shadow repo) as the workspace root. Deploy-breaking hazard, closed.
- **`686c97a` left-bar model switcher** (`components/ModelSwitcher.tsx` + `layout.tsx`) — the CORE concept, per Oscar: the four models ARE the product (a live eval of design taste). Persistent switcher = neutral eval-frame chrome (dark, mono labels, gold active accent, deliberately not a 5th design), left rail on desktop / top strip on mobile, `usePathname` active state, links to `/compare`. `<main>` offset so it frames but never covers a design. Running for pixel review at `localhost:3001`.

## Verified
- **`npm run build`** → `✓ Compiled successfully in 4.5s`, `✓ Generating static pages (10/10)`, **no workspace-root warning** (was present before the config fix). All routes prerender: `/ /compare /fable /haiku /opus /sonnet`.
- **Asset integrity** — all 8 `/projects/*.{png,jpg}` referenced in `data.ts` exist in `public/projects/`. **0 missing.**
- **Metadata** — `title`, `description`, `openGraph`, `metadataBase (oscarmorke.com)` all set in `layout.tsx`.
- **Overclaim grep** — only remaining `finalist`/`#1` hit is NFT Safe Launch's `ethglobal finalist` (legitimate). No false numbers left.

## Reverted
Nothing broke. Two things I deliberately did NOT change (out of scope / needs the sheet):
- Did **not** blanket-rename RELAY → FAVOUR (branding decision — logged below).
- Did **not** touch NFT Safe Launch's "finalist" — §13 says ETH NYC '22 was "1st place," which would make "finalist" an *under*claim; can't reconcile without the prize sheet.

## Needs Oscar (human-only gates)
1. **~~Pick ONE design direction~~ — WRONG (corrected by Oscar).** The four models ARE the product: a live eval of design taste, toggled via the new left-bar switcher. Do NOT collapse to one. Instead: **pixel-review the switcher** (`localhost:3001`) and call adjustments (width, position, labels, add per-model personality descriptors, accent).
2. **RELAY → FAVOUR** — the live product is FAVOUR; the portfolio project entry + `relay.png` still say RELAY (the World Build win *was* as RELAY). Decide the framing.
3. **NFT Safe Launch** — portfolio says "finalist"; §13 says ETH NYC '22 was "1st place." Reconcile against your sheet.
4. **The $188K / 42 ETH figure** — I used the mindmap's stated team value; §13 flags the cash tally as "still to reconcile." Verify before it's public.
5. **Deploy gates (not crossed):** push the branch, merge, deploy to `oscarmorke.com` via Vercel. Also: the home-dir shadow git repo still tracks duplicate copies — worth a clean-up so it can't bleed into builds again.

## Oscar's portfolio vision (Jul 13 live review) — "the playground"
The portfolio = **our little playground**: highlight EVERYTHING being shipped, all the fun stuff, framed as a live eval of design taste across four Claude models. Build queue (each is a design-taste + pixel-review chunk, done WITH Oscar):
1. ✅ Switcher order → **fable → opus → sonnet → haiku** (`ee797ed`).
2. ✅ **Mobile scroll cut** — full-viewport sections collapse to content height <768px (`ee797ed`). *Needs Oscar's mobile-width eyeball; if `auto` feels tight, cap at ~55vh.*
3. ✅ **Categories framed full-time · agentic · hackathons** (`81ce449`) — all four models already grouped by track; relabeled the single `TRACKS` source + pointed haiku's file-tree at the labels.
4. ⏳ **Fun / heritage stories — YOURS to tell (blocked on you, by design).** The `portfolio-build-kit.md` (Jul 8, `01 Projects/Hackathons/night-run-2026-07-08/`) already scaffolds these as `[FUN STORY SLOT]`s: declined Antler's $95K · Coinbase "Digital Vinyl" rescue · frag-movies-at-13 · DJ house/techno · the 5-terminal workflow. It also wants 3-5 deep case studies (the "being sourced" differentiator). Both are content + voice, not code.
5. ⏳ **Graphs / dataviz + design-library feel** — natural first chart: prizes/ETH over time from `HACKATHON_TIMELINE`. Needs your taste on which viz + which model carries it.
6. ⏳ **Media pass** — better videos + images per model. Needs your assets.

## Scorecard
**8/10.** Real + verified (build green after every change): number-hygiene, build-root fix, the **model switcher** (the core four-model concept made navigable), mobile density, and the **full-time · agentic · hackathons** framing — 5 commits, all on the branch, none pushed. Scaffold/blocked (all need you, by design): the fun stories (your voice), graphs, media, RELAY→FAVOUR naming, and the deploy. The portfolio went from *lying + undeployed* to honest, building green, and actually navigable as the four-model taste eval it's meant to be.

## Next prompt
> Paris Portfolio is number-clean + build-green on branch `nightrun/portfolio-hygiene-2026-07-13`, and the four-model concept now has a persistent left-bar switcher (the eval frame). Next: (1) pixel-tune the switcher with Oscar (width/labels/per-model descriptors/accent), (2) improve videos + images per model, (3) add copy + the fun/heritage stories, (4) resolve RELAY → FAVOUR naming, (5) verify $188K / 42 ETH vs the sheet, then merge + deploy to oscarmorke.com. `npm run build` after each change to stay green.
